replicaCount: 1
yamlApplicationConfig:
  kafka:
    clusters:
      - name: my-kafka
        bootstrapServers: my-cluster-kafka-bootstrap:9092
        #        kafkaConnect:
        #          - name: my-connect-cluster
        #            address: http://my-connect-cluster-connect-api:8083
        kafka-connect:
          - name: my-connect-cluster
            address: http://my-connect-cluster-connect-api:8083
resources:
  limits:
    cpu: 800m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 1Gi
