ingress:
  enabled: false
  ingressClassName: "nginx"
  hostname: minio.local
metrics:
  enabled: true
console:
  enabled: true
  ingress:
    enabled: true
    ingressClassName: "nginx"
    hostname: minio-console.local
auth:
  forceNewKeys: true
  useSecret: true
mode: standalone