ingress:
  enabled: true
  ingressClassName: "nginx"
  hostname: minio.local
metrics:
  enabled: true
console:
  enabled: true
  ingress:
    enabled: true
    ingressClassName: "nginx"
    hostname: minio-console.local
auth:
  rootPassword: password
  rootUser: admin
  forceNewKeys: true
  useSecret: false